import { MarkdownOptions } from './markdown/markdown';
import { PageData } from './shared';
interface MarkdownCompileResult {
    vueSrc: string;
    pageData: PageData;
    deadLinks: string[];
}
export declare function createMarkdownToVueRenderFn(root: string, srcDir: string, options: MarkdownOptions | undefined, pages: string[]): (src: string, file: string, publicDir: string) => MarkdownCompileResult;
export {};
